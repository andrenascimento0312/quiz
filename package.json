{
  "name": "quiz-realtime",
  "version": "1.0.0",
  "description": "Sistema de quiz em tempo real com React e Socket.IO",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "nodemon server/index.js",
    "client:dev": "cd client && npm run dev",
    "build": "echo 'Backend não precisa de build'",
    "start": "node server/index.js",
    "migrate": "node server/scripts/migrate.js",
    "seed": "node server/scripts/seed.js",
    "setup": "npm install && cd client && npm install && cd .. && npm run migrate && npm run seed",
    "postinstall": "npm run init:auto",
    "init:auto": "node -e \"process.env.NODE_ENV === 'production' ? require('./server/scripts/init-production').initProduction() : console.log('🏠 Desenvolvimento: pule a inicialização automática')\"",
    "init:production": "node server/scripts/init-production.js",
    "migrate:users": "node server/scripts/migrate-users.js",
    "deploy:railway": "npm run init:production && npm start",
    "deploy:render": "npm run init:production && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.0",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.10.0",
    "joi": "^17.9.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^8.2.0"
  },
  "keywords": ["quiz", "realtime", "socket.io", "react"],
  "author": "",
  "license": "MIT"
}